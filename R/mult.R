#' Multiply together two numbers.
 #'
   #' @param x A number.
   #' @param y A number.
   #' @return The product of \code{x} and \code{y}.
   #' @examples
   #' mult(1, 1)
   #' mult(10, 1)
   mult <- function(x, y) {
         x * y
    }
